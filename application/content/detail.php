<?php
    /*
    * This used to load detail content of the page need (subject or topic) via ajax
    * It depends on variable $_GET['view'] to determine what to do
    * the ajax call url: application/content/detail.php?view=.....
    */

    $view = $_GET['view'];
?>